# Phase 11 — Build the upload dropzone component

Build the upload dropzone component for the sidebar: a drag-and-drop area with a 'browse files' button, accepting PDF, DOCX, and TXT files, matching the dashed-border style from the mockup. Use mock behavior for now (log the selected file) — no backend wiring yet.
