# Phase 12 — Build the document library component

Build the document library component that lists uploaded documents as cards, each showing the file name, a status badge ('Ready · N chunks', 'Indexing · step X of 4', or 'Failed'), and a delete icon. Use an array of mock sample documents for now — no backend wiring yet.
