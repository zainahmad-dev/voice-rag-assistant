# Phase 13 — Build the voice orb component

Build the voice assistant orb component: a circular mic button with three visual states — idle, listening (pulsing ink-teal ring), and speaking (faster pulsing gold ring) — plus a status label beneath it. Make the state controllable via a prop so it can be wired to real VAPI call state later.
