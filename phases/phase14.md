# Phase 14 — Build the conversation/chat component

Build the conversation history component: user messages as right-aligned filled bubbles, assistant messages as left-aligned outlined cards with a small source-attribution line (document name and similarity score) underneath. Render from a mock list of messages for now.
