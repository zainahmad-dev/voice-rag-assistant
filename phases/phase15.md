# Phase 15 — Implement the dark/light mode toggle

Wire up a real, working dark/light mode toggle for the dashboard: a button in the header that switches the theme, persists the choice (e.g. in localStorage), and respects the user's system preference on first load.
