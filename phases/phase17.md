# Phase 17 — Build the text extraction utility

Implement a text extraction utility function that takes a file buffer and a file type ('pdf' | 'docx' | 'txt') and returns the extracted plain text, using pdf-parse for PDFs and mammoth for DOCX files.
