# Phase 18 — Build the chunking utility

Implement a text chunking utility function that splits a long string into overlapping chunks (configurable chunk size and overlap, defaulting to about 1500 characters with 200 characters of overlap), breaking on sentence or word boundaries where possible instead of mid-word.
