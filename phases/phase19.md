# Phase 19 — Build the embedding utility

Implement an embedding utility using the Google Gemini text-embedding-004 model: one function to embed a single string (for user questions) and one function to embed an array of chunks with limited concurrency (for document ingestion).
