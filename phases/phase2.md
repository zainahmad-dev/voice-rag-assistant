# Phase 2 — Install project dependencies

In the voice-rag-assistant project, install these dependencies: @supabase/supabase-js, @google/generative-ai, groq-sdk, pdf-parse, mammoth, @vapi-ai/web, zustand, uuid, @types/uuid, and lucide-react. Confirm each package installs without errors and list what each one will be used for in this project.
