# Phase 23 — Build the context assembly function

Implement a function that takes the retrieved chunks from the vector search step and formats them into a single labeled context block (e.g. '[Source 1]', '[Source 2]') ready to be inserted into an LLM prompt.
