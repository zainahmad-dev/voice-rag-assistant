# Phase 25 — Build the text query API route

Implement the /api/query API route: accept a JSON body with a question (and optional document id filter), call the answer generation function, and return the answer and sources as JSON. This route is for testing the RAG pipeline via text before voice is wired in.
