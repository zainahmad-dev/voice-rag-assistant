# Phase 26 — Set up the VAPI account and assistant config

Walk me through creating a VAPI account and getting the public and private API keys. Then write the inline VAPI assistant configuration object for this app: a system prompt instructing the assistant to always call an answerQuestion function tool instead of answering from its own knowledge, the transcriber and voice provider settings, and the function tool definition pointing at our webhook URL.
