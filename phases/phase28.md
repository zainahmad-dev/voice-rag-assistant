# Phase 28 — Connect the mic button to the VAPI SDK

Wire the voice orb component to the real @vapi-ai/web SDK: starting a call when the orb is pressed, stopping it when pressed again, and initializing the client with the public key and the assistant config from the previous phase.
