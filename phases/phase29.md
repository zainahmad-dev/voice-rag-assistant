# Phase 29 — Handle voice call states

Extend the voice integration to listen for VAPI's call events (call started, speech started/stopped, call ended, errors) and reflect them as the orb's idle/listening/speaking visual states, plus surface any call errors to the user.
