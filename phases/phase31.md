# Phase 31 — Add loading states and error handling

Add clear loading indicators for uploading and indexing, and add user-facing error messages for every failure case: unsupported file type, file too large, upload failure, extraction failure, embedding failure, empty vector search results, and voice call errors. Errors should say what happened and what to do next, without technical jargon.
