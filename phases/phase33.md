# Phase 33 — Implement conversation history display

Wire the conversation component to a shared state store (e.g. Zustand) so both voice interactions and any text-based testing queries appear in the same conversation history, in order, with correct source attribution for each assistant message.
