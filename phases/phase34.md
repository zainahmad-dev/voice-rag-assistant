# Phase 34 — Create the PWA manifest

Create a manifest.json for this app with the app name, short name, description, theme colors matching the design system, display mode 'standalone', and icon entries for multiple sizes. Link it from the root layout.
