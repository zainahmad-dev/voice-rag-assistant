# Phase 35 — Set up the service worker

Set up a service worker (using next-pwa or a custom implementation) that caches the app shell and static assets for offline access, and register it from the app. Explain what will and won't work offline given this app depends on live API calls.
