# Phase 36 — Build the install prompt component

Build a small install-prompt component that listens for the browser's beforeinstallprompt event and shows a subtle banner or button inviting the user to install the app to their home screen or desktop, matching the design system's visual style.
