# Phase 41 — Configure Vercel environment variables

List every environment variable that needs to be added in the Vercel project settings for this app to work in production, and explain any differences from the local .env.local values (e.g. webhook URLs that must point at the production domain instead of localhost).
