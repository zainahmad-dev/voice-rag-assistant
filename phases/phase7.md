# Phase 7 — Set up environment variables

List every environment variable this project needs — Supabase URL, Supabase anon key, Supabase service role key, Gemini API key, Groq API key, VAPI public key, VAPI private key — with a one-line description of each and where to get it. Create a .env.local.example file with all of these as placeholders, and explain which ones are safe to expose to the browser (NEXT_PUBLIC_ prefix) and which must stay server-only.
