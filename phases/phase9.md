# Phase 9 — Implement global styles and fonts

Using the confirmed design tokens, update the Next.js project's global CSS and font loading: load Space Grotesk, Inter, and JetBrains Mono via next/font/google, define CSS variables for both light and dark mode colors, and add the keyframe animations for the listening orb's pulse effect, including a prefers-reduced-motion fallback.
